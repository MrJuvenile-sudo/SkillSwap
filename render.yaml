services:
  - type: web
    name: skillswapx
    env: node
    plan: free
    region: oregon
    buildCommand: npm install && npm run db:init
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
